<Window x:Class="gymTracker.Pages.customMbx"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:gymTracker.Pages"
        mc:Ignorable="d"
        Title="customMbx" Height="175" Width="250" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <!-- pink colour -->
        <SolidColorBrush x:Key="PinkBrush" Color="#ED79D5"/>

        <!-- pink rounded button style -->
        <Style x:Key="PinkRoundedButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PinkBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="10"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="15"
            BorderBrush="{StaticResource PinkBrush}"
            BorderThickness="3"
            Background="White">

        <Grid Margin="20">
            <StackPanel VerticalAlignment="Center"
                        HorizontalAlignment="Center">

                <TextBlock x:Name="MessageText"
                           Text="Message goes here"
                           TextAlignment="Center"
                           FontSize="16"
                           TextWrapping="Wrap"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>

                <Button Content="OK"
                        Width="100"
                        Height="40"
                        Margin="0,10,0,0"
                        Style="{StaticResource PinkRoundedButton}"
                        Click="OkBtn_Click"/>
            </StackPanel>
        </Grid>

    </Border>
</Window>
